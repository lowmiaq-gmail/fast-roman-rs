# SEO and Distribution Report

## Search intent and keywords

- Primary: `roman Python Rust`, `fast roman numeral converter`, `roman drop-in replacement`.
- Secondary: `PyO3 roman numerals`, `roman 5.2 compatible`, `Python Roman numeral CLI`.
- Transactional installation intent is served by the repository title, first README paragraph, and `pip install fast-roman-rs` example.
- Migration intent is served by explicit namespace/API compatibility and rollback instructions.

## Repository and PyPI optimization

- Proposed GitHub description: `Fast Rust-backed drop-in replacement for roman 5.2 with PyO3 abi3 wheels and a pure-Python fallback.`
- Proposed topics: `roman`, `roman-numerals`, `python`, `rust`, `pyo3`, `maturin`, `drop-in-replacement`, `conversion`, `cli`, `performance`.
- PyPI metadata includes targeted keywords, complete project URLs, supported Python classifiers, Rust classifier, license expression, and a Markdown README.
- README headings expose installation, compatibility, migration, rollback, CLI, benchmark, and verification terms to search indexing.

## Claims gate

- No ranking, traffic, star, adoption, or download claim is made.
- No benchmark multiple is promoted before the exact release artifact is measured.
- Compatibility is described against frozen upstream `roman==5.2` and backed by executable gates, not an unqualified future-version claim.
- No issues, PRs, accounts, stars, or communities are spammed or fabricated.

## Ecosystem entry points

- Public GitHub repository and formal Release.
- Public PyPI project with OIDC provenance/attestations.
- Standard package metadata and project URLs.
- Migration and rollback documentation for legitimate adopters.

External promotion beyond these owned, compliant surfaces is not claimed until actually performed and evidenced.
